#### 浏览器在一次 HTTP 请求中，需要传输一个 4097 字节的文本数据给服务端，应采用哪种方式?
IndexdDB 是 HTML5 的本地存储，把一些数据存储到浏览器（客户端）中，当与网络断开时，可以从浏览器中读取数据，用来做一些离线应用。

Cookie 通过在客户端 ( 浏览器 ) 记录信息确定用户身份，最大为 4 kb 。

url 参数用的是 get 方法，从服务器上获取数据，大小不能大于 2 kb 。

Session 是服务器端使用的一种记录客户端状态的机制 。

post 是向服务器传送数据，数据量较大。

local Storage 也是 HTML5 的本地存储，将数据保存在客户端中（一般是永久的）


```js
for(let i=0;i<2;i++){
    setTimeout(function(){
    console.log(i)
    },100);
}
for(var i=0;i<2;i++){
    setTimeout(function(){
        console.log(i)
    },100);
}
```
js是单线程，所以等待输出了等一个函数才开始第二个，所以是0 1，然后第二个函数由于定时函数生效，然而for循环是一瞬间完成的，所以for循环已经走完了，然而延时函数才准备输出，此时i为2，所以输出了两次i=2

#### 下列的值为
```js
var result = "75team2017".match(/\d+\w*/g);
```
[“75team2017”]

\d 匹配一个数字,等价于[0-9]

\w 匹配字母、数字或者下划线,等价于 [A-Za-z0-9_]

+匹配前面一个表达式 1 次或者多次

* * 匹配前一个表达式 0 次或多次

/g全局匹配

因为"+"和" * "都是贪婪匹配，它们会尽可能多地匹配字符，  所以"\d+"匹配"75"，"\w*"匹配"team2017"，而"/g"就没有表演的机会了。 


#### 以下哪一条Javascript代码会产生运行错误？
var obj=( );
A，错误
var obj=[ ];
B，声明obj为数组对象
var obj={ };
C，声明obj为对象
var obj=/ /;
D，声明obj为正则对象

****任何对象转为布尔值，都为得到 true**（**切记！在 JS 中，只有 0，-0，NaN，""，null，undefined 这六个值转布尔值时，结果为 false** ）。**

#### 在ES6规范中，以下类型哪些属于基本数据类型（**String Null Undefined Symbol**）






