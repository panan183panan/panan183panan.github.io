#### 创建vue3项目
- 使用cli脚手架创建项目
```shell
vue create vue3 test
```
- 使用vite创建项目
```shell
npm init vite-app project-name
```
	
```shell
npm install
```
**优势**：
开发环境中，无需打包操作，可快速冷启动
轻量快速的热重载（HMR）
真正的按需编译，不在等待应用编译完成

